// scripts/app.js
// Generic app bootstrapping. Resizer and dashboard logic live in encounter-dashboards.js.
document.addEventListener('DOMContentLoaded', () => {
  // Placeholder for any global app setup that isn't dashboard-specific.
  // (Intentionally minimal to avoid conflicting with encounter-dashboards.js)
});
